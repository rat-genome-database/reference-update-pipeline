Jul 31, 2026
  RetractedReferences: the downloaded Retraction Watch csv is stored gzipped (65 MB down to 14 MB)
  RetractedReferences: implemented the retraction itself -- a retracted reference is withdrawn, "RETRACTED: " is
    prepended to its title, and its annotations are moved from FULL_ANNOT to the new FULL_ANNOT_RETRACTED table
    (whatever the reference's status), with every moved annotation written to retracted_annotations.log for curators;
    PhenoMiner/gene expression studies built on a retracted reference are reported only, not yet modified;
    guarded by a 'dryRun' bean property, ON by default
  RetractedReferences: REFERENCES_RETRACTED is synchronized incrementally (insert/update/delete) instead of
    being reloaded from scratch; keyed on the new RECORD_ID column, which carries a unique index

Jul 30, 2026
  added RetractedReferences module (--retractedReferences): loads retraction data published by Crossref
    (the Retraction Watch database) into the new REFERENCES_RETRACTED table, and reports retracted references
    that are still active in RGD, and retracted references already withdrawn in RGD that still have annotations;
    reports are ordered by annotation count then retraction date, and break the annotations down by
    aspect/ontology and by data source; read only as far as curated data goes -- nothing is withdrawn,
    retitled or deleted

Jun 30, 2026
  migrated FileDownloader to FileDownloader2; removed unused dependencies (commons-net, httpclient, jaxen); AppConfigure.xml migrated from Spring DTD to XSD schema; removed orphan AuthorQC; updated dependencies

May 12, 2026
  CLI: normalized all options to '--' prefix (except '-?'); fail loud on unknown options or when no work was requested; updated dependencies

Apr 16, 2026
  added memory usage monitoring; fixed FileReader resource leak in parseXMLfile()

Sep 04, 2025
  removed custom dependency to EntrezGeneLoading.jar

Jun 09, 2025
  updated dependencies
  
Apr 04, 2025
  added import of missing references for HPO annotations

Mar 31, 2025
  renamed github branch 'master' to 'main'

Feb 18, 2025
  renamed distro to 'reference-update-pipeline'

Oct 18, 2024
  added module to import references from PMIDs found in HTPDATASET file submitted to the Alliance

Jul 01, 2024
  updated to java 17

Apr 18, 2023
  refactored to simplify code

Jan 27, 2023
  upgraded commons-dbcp from ver 1 to ver 2

Jan 10, 2023
  upgraded build script to the most recent gradle syntax
